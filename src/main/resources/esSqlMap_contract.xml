<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="esContractSql">
	<sql id="findByPage" >
		select *
		from sucss_union_contract_detail/sucss_union_contract_detail
		where status = #{status}
		order by updateDate desc
		limit #{offset},#{size}

	</sql>




</mapper>

